<!DOCTYPE html>
<html>
<head>
  <title>MessageBoard</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <nav class="navbar">
    <div class="container">
      <div class="navbar-header">
        <a class="navbar-brand" href="#">Message Board</a>
      </div>
    </div>
  </nav>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
<div class="panel panel-default">
  <div class="panel-heading">
    メッセージ一覧
  </div>
    
  <div class="panel-body">
    <% @messages.each do |message| %>
      <span class="label label-info"><%= message.name %></span>
      <%= message.body %>
      <br>
    <% end %>
  </div>
</div>
<%= form_for @message, :html => {:class => 'form-horizontal'} do |f| %>
     <%= f.label :name, "名前", class: "col-sm-2 control-label" %>
    <%= f.text_field :name, class: "col-sm-10", :maxlength => "5", :placeholder => "名前を入力(15字以内)" %>
    <%= f.label :body, "内容", class: "col-sm-2 control-label"%>
    <%= f.text_area :body, class: "col-sm-10", :maxlength => "100", :placeholder => "コメントを入力(100字以内)" %>
    <%= f.submit class: "btn btn-sm btn-primary" %>
    
<% end %>